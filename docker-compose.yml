services:
  recommender:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      PYTHONPATH: /app
    command: uvicorn src.api.app:app --host 0.0.0.0 --port 8000

